# GPT Prompt Coach

A working demo built for a FDS interview exercise.
A prompt-quality coach that scores prompts against five dimensions of strong
prompt engineering (role, task clarity, context, constraints/format, examples),
suggests a rewrite, and shows how prompt quality changes the output a user gets.

Single self-contained HTML file. Deterministic scoring, no external dependencies.
